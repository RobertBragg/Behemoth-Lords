using UnityEngine;
using System.Collections;

public class MountainGiant : PlayerStats {

	// Use this for initialization
	void Start () {		
		SetStrength(4);
		SetMagic(0);
		SetDefense(2);
		SetSpeed(3);
		SetSize(2);
		SetHoard(2);
		SetDomain(2);
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
